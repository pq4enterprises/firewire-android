<?xml version="1.0" encoding="utf-8"?>
<!-- 2026 design system comment row, design screen 10 · COMMENTS:
     avatar (red-tint person fallback) with the name + timestamp block anchored
     to the avatar's end and top-aligned (iOS FWCommentCell header row), body
     copy, optional images strip, REPLY action + replies count; hairline
     divider. Reply rows indent via start padding set by the adapter. -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cl_comment_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="16dp">

    <!-- fallback circle shows through until Glide loads a real avatar -->
    <FrameLayout
        android:id="@+id/fl_avatar"
        android:layout_width="42dp"
        android:layout_height="42dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/fw_avatar_fallback_bg"
            android:padding="8dp"
            android:src="@drawable/fw_ic_user"
            android:importantForAccessibility="no" />

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/profile_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </FrameLayout>

    <TextView
        android:id="@+id/tv_profile_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="11dp"
        android:layout_marginEnd="8dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="@color/fw_text"
        android:textSize="@dimen/text_14"
        android:fontFamily="@font/poppins_semibold"
        app:layout_constraintStart_toEndOf="@+id/fl_avatar"
        app:layout_constraintEnd_toStartOf="@+id/tv_date_time"
        app:layout_constraintTop_toTopOf="@+id/fl_avatar"
        tools:text="John Mike" />

    <TextView
        android:id="@+id/tv_date_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAllCaps="true"
        android:textColor="@color/fw_muted"
        android:textSize="@dimen/fw_text_chip"
        android:fontFamily="monospace"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/tv_profile_name"
        app:layout_constraintBottom_toBottomOf="@+id/tv_profile_name"
        tools:text="10 OCT | 10:15 AM" />

    <TextView
        android:id="@+id/tv_comments"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="11dp"
        android:textColor="@color/fw_text"
        android:textSize="@dimen/text_14"
        android:fontFamily="@font/poppins_medium"
        android:lineSpacingMultiplier="1.15"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/fl_avatar"
        tools:text="Lorem Ipsum is simply dummy text of the printing and typesetting industry." />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_images"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:orientation="horizontal"
        android:visibility="gone"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/item_image"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tv_comments" />

    <!-- REPLY action (left) + replies count (right), design screen 10 -->
    <TextView
        android:id="@+id/tv_reply"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:minHeight="32dp"
        android:gravity="center_vertical"
        android:paddingEnd="12dp"
        android:text="@string/fw_reply"
        android:textAllCaps="true"
        android:textColor="@color/fw_muted"
        android:textSize="@dimen/fw_text_caption"
        android:fontFamily="@font/poppins_bold"
        android:letterSpacing="0.03"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/rv_images" />

    <TextView
        android:id="@+id/tv_replies"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:drawablePadding="7dp"
        android:gravity="center_vertical"
        android:textAllCaps="true"
        android:textColor="@color/fw_muted"
        android:textSize="@dimen/fw_text_caption"
        android:fontFamily="@font/poppins_bold"
        android:letterSpacing="0.03"
        android:visibility="gone"
        app:drawableStartCompat="@drawable/fw_ic_comment"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/tv_reply"
        app:layout_constraintBottom_toBottomOf="@+id/tv_reply"
        tools:visibility="visible"
        tools:text="4 REPLIES" />

    <View
        android:layout_width="match_parent"
        android:layout_height="@dimen/fw_hairline_width"
        android:layout_marginTop="8dp"
        android:background="@color/fw_hairline"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tv_reply" />

</androidx.constraintlayout.widget.ConstraintLayout>
